# سناریوی تحلیلی جدید - فراتر از دیتا مارت فعلی

**توسعه‌دهنده:** سارینا فتحی
**تاریخ:** ۱۴۰۵/۰۵/۲۵

---

## ❓ سناریو:

*"می‌خواهیم بدانیم مشتریانی که در ۳ ماه اخیر حداقل ۲ بار از فروشگاه‌های مختلف خرید کرده‌اند، چه الگوی خریدی دارند و آیا تمایل بیشتری به خرید محصولات گران‌قیمت دارند یا نه؟"*

---

## 🚫 چرا با دیتا مارت فعلی قابل پاسخ نیست؟

### محدودیت‌های دیتا مارت فعلی:

1. **نداشتن بُعد مشتری (Dim_Customer):**  
   دیتا مارت فعلی فقط فروشگاه و محصول رو داره. برای تحلیل مشتریان، نیاز به جدول `dim_customer` با اطلاعاتی مثل `customer_id`, `registration_date`, `city`, `age_group` داریم.

2. **عدم وجود تاریخچه رفتار مشتری:**  
   جدول حقیقت فعلی فقط فروش رو ذخیره میکنه. برای تحلیل «تعداد خرید در ۳ ماه اخیر»، نیاز به فیلد `customer_key` توی `fact_sales` و قابلیت فیلتر روی بازه‌ی زمانی داریم.

3. **نیاز به ابعاد بیشتر:**  
   برای تشخیص «خرید از فروشگاه‌های مختلف»، باید بُعد فروشگاه رو با مشتری ترکیب کنیم که نیاز به طراحی چندبعدی پیچیده‌تری داره.

---

## ✅ راه‌حل پیشنهادی برای توسعه دیتا مارت:

### اضافه کردن جدول‌های جدید:

```sql
-- ۱. بُعد مشتری (Dim_Customer)
CREATE TABLE dim_customer (
    customer_key SERIAL PRIMARY KEY,
    customer_id INT UNIQUE,
    full_name VARCHAR(255),
    email VARCHAR(255),
    city VARCHAR(100),
    registration_date DATE,
    age_group VARCHAR(20),  -- '18-25', '26-35', '36-50', '50+'
    etl_created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ۲. به‌روزرسانی Fact_Sales با کلید مشتری
ALTER TABLE fact_sales ADD COLUMN customer_key INT REFERENCES dim_customer(customer_key);